import mapLanguages from './mapLanguages';

describe('mapLanguages', () => {
  it('should', async () => {
    expect(mapLanguages('nb')).not.toBeUndefined();
  });

  it('should map', async () => {});
});
